<h1>Transactions Grouped By Category</h1>
<table class="table table-bordered table-striped">
    <thead>
        <tr>
            <th>category</th>
            <th>amount</th>
        </tr>
    </thead>
    <tbody>
        <% results.map(item => {%>
            <tr>
               <td> <%= item._id %> </td>
               <td><%= item.amount %></td>
            </tr>
            <% }) %>

    </tbody>
</table>
